# Cin5_recommend
Setup & Run
Install dependencies
pip install -r requirements.txt
Run the application locally
pip install -r requirements.txt
uvicorn api:app --host 0.0.0.0 --port 8000
If the local API is not accessible, the application is already deployed and running online.
You can test the API here:
https://cine5-a2ia-14.onrender.com
(The API will remain available until 2027)
